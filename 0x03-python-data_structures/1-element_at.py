#!/usr/bin/python3
def element_at(my_list, idx):
    lenlist = len(my_list)
    if idx < 0 and idx > lenlist:
        return None
    for x in range(len(my_list)):
        if idx == counter:
            return my_list[idx]
