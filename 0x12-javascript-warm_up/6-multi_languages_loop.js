#!/usr/bin/node
/*
   Loops, arrays in javascript
*/
const lang = ['C is fun', 'Python is cool', 'Javascript is amazing'];
let count = 0;
while (count < lang.length) {
  console.log(lang[count]);
  ++count;
}
